declare module '@truzzt/mui-lib/truzztTheme' {
  import { Theme } from '@mui/material/styles'; // Import the necessary types

  // Declare the type for the 'theme' object
  const theme: Theme;

  // Export the 'theme' object type
  export { theme };
}
